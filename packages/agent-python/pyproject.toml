[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bolthub"
version = "0.1.1"
description = "L402 client for AI agents — pay Lightning invoices automatically to access paywalled APIs"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
dependencies = ["httpx>=0.24"]
keywords = ["l402", "lightning", "bitcoin", "micropayments", "ai-agent", "paywall"]

[project.optional-dependencies]
dev = ["pytest"]

[project.urls]
Homepage = "https://bolthub.ai"
Repository = "https://github.com/signaltech-org/bolthub-sdk"
Issues = "https://github.com/signaltech-org/bolthub-sdk/issues"

[tool.pytest.ini_options]
testpaths = ["tests"]
